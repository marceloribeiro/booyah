{{model_name}} = {{model_name.camelize()}}.find(self.params['id'])
        deleted_id = {{model_name}}.destroy()

        return self.respond_to(
            html=lambda: self.redirect('/{{model_name.pluralize()}}'),
            json=lambda: self.render({ "deleted": True, "deleted_id": deleted_id })
        )